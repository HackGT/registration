import * as mocha from "mocha";
import { expect } from "chai";
import * as request from "supertest";

import * as Registration from "../app";

describe("Logging in", () => {
	describe("OAuth", () => {
		it("Logs in with Google");
		it("Logs in with Facebook");
	});
});
