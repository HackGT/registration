git:
    branch: master

wants:
    mongo: true

health:
  path: /version

secrets:
  - ADMIN_KEY_SECRET
  - SESSION_SECRET
  - EMAIL_KEY
  - STORAGE_ENGINE_OPTIONS
  - GROUND_TRUTH_URL
  - GROUND_TRUTH_ID
  - GROUND_TRUTH_SECRET

env:
  STORAGE_ENGINE: s3
  COOKIE_SECURE_ONLY: true
  EMAIL_FROM: "HackGT Team <hello@hackgt.com>"
  PRODUCTION: true
  ADMIN_EMAILS:
    - stephanie@hack.gt
    - evan@hack.gt
    - claudia@hack.gt
    - catalyst@hack.gt

